name: xcross
description: >-
  Build, run, and hot-reload Flutter iOS apps from Linux without Xcode, by
  driving the xtool CLI for sign/install and re-implementing the
  Flutter build + iOS 17 CoreDevice launch pipeline in Dart.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  archive: ^4.0.9
  args: ^2.7.0
  async: ^2.13.1
  completion: ^1.0.2
  build_cli_annotations: ^2.1.1
  cli_util: ^0.5.1
  meta: ^1.18.3
  path: ^1.9.1
  tar: ^2.0.2
  yaml: ^3.1.3
  web_socket_channel: ^3.0.3
  posix: ^6.5.0

dev_dependencies:
  build_cli: ^2.2.12
  build_runner: ^2.15.0
  lints: ^6.1.0
  test: ^1.31.2
